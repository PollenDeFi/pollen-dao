/* global after, afterEach, artifacts, before, beforeEach, contract, describe, it, web3 */
const Pollen = artifacts.require("IAdminUpgradeabilityProxy");

describe.skip('PollenDAO (proxy)', () => {
    // TODO: write tests for the PollenDAO proxy
    describe('being a "transparent proxy"', () => {
    });
    describe('being an "upgradable proxy"', () => {
    });
    describe('being a proxy for PollenDAO', () => {
    });
});
